# introduccion-progrmacion-cientifica-

The cleaned game dataset is available as Apache Parquet at
`data/game_info_cleaned.parquet`. It contains 474,417 rows and 27 columns.

Install the required Python packages with:

```powershell
python -m pip install -r requirements.txt
```